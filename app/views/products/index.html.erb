<p id = "notice"><%= notice %></p>
<p style="color: green"><%= notice %></p>

<h1>Books</h1>


<style>
table {text-align: center; # /* 文字を中央揃えにする */
 } 
th, td {
  padding: 8px;  # /* セルの余白を設定 */
}
.title-header {
  text-align: left;
}
</style>

<table>
  <thead>
    <tr>
      <th>.*★📖Title*.⁺:</th>
      <th>👤Author</th>
      <th>⌚Publushed on</th>
      <th>📚Status</th>
      <th>💴Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td class="title-header"><%= link_to book.title, product_path(book) %></td>
        <td><%= book.author %></td>
        <td><%= book.publushed_on %></td>
        <td><%= book.status %></td>
        <td><%= book.price %></td>
      </tr>
    <% end %>
  </tbody>
</table>

