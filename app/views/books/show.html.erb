<p style="color: green"><%= notice %></p>

<style>
.button {
  font-size: 15px;
}
</style>

<%= render @book %>

<p>
  <strong>タグ</strong>
  <%= @book.tags.map(&:name).join(', ') %>
</p>

<div>
  <%= link_to "🖊Edit this book", edit_book_path(@book) %> |
  <%= link_to "🔙Back to books", books_path %>

  <p><%= button_to "Destroy this book", @book, method: :delete, class:"button"%></p>
</div>
